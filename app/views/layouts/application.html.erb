<!DOCTYPE html>
<html>
  <head>
    <title>Wine Counselor</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= javascript_importmap_tags %>

     <%= favicon_link_tag "logo.png", rel: "icon", type: "image/png" %>
  </head>

  <body class="container-fluid d-flex flex-column home-page <%= 'body' unless current_page?(root_path) || current_page?(new_caviste_offer_path) || controller.controller_name == "offers" && controller.action_name == "edit" %>">
    <%= render "shared/flashes" %>
    <%= render "shared/navbar" %>
    <%= yield %>
    <%= render 'layouts/footer'%>
  </body>
</html>
